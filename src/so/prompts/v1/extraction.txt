The document is provided as one image per page. Read all pages, including charts, tables and figures, and fill every field of the JSON schema, following each field's description. Use null when the document does not state a value. For list fields, extract every item the document mentions; do not stop after a few. Numbers must be given as numbers, and dates written exactly as they appear in the document. Respond with JSON only.
